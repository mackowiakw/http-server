#pragma once
#include <string>
#include "httpMethods.hpp"

// namespace own { 
    const int SERVER_PORT{ 1234 };

    // Don't add / (slash) at the end of path
    const std::string ROOT_FOLDER{ "SERVER_ROOT_FOLDER" };
    
//} // namespace own
