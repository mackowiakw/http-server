#pragma once

#include <string>

const int SERVER_PORT{ 1234 };

// WSL path to Host's (Windows) folder "C:/Users/WiktorMackowiak/Desktop/http"
// Don't add / (slash) at the end of path
const std::string ROOT_FOLDER{ "/mnt/c/Users/WiktorMackowiak/Desktop/http" };
